# SAR compute image dependencies - adapted from LAwildfireSAR's own
# requirements.txt (same pyroSAR/RTC processing stack).
#
# matplotlib/contextily/pillow were dropped early in this project's build
# (dashboard's interactive MapLibre map was meant to replace static
# figures) - reinstated 2026-07-31 after comparing real output against
# LAwildfireSAR's own figures: the before/after backscatter comparison
# specifically can't be shown by a vector map at all, only a rendered
# raster image. See pipeline/figures.py.
#
# GDAL requires a pre-built wheel on Windows for local dev; inside the
# Docker image (Ubuntu) `pip install GDAL==<version>` matching the
# system's apt-installed GDAL works directly - see Dockerfile.

boto3==1.43.36
botocore==1.43.36
contextily==1.6.2
geopandas==1.1.3
httpx==0.28.1
matplotlib==3.10.6
numpy==2.4.2
pillow==11.3.0
psycopg2==2.9.12
pyproj==3.7.2
pyroSAR==0.36.2
python-dotenv==1.2.2
PyYAML==6.0.3
rasterio==1.5.0
rasterstats==0.21.0
scipy==1.18.0
shapely==2.1.2
spatialist==0.19.0
