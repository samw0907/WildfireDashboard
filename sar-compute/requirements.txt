# SAR compute image dependencies - adapted from LAwildfireSAR's own
# requirements.txt (same pyroSAR/RTC processing stack), trimmed:
# matplotlib/contextily/pillow dropped since this produces data outputs
# (GeoJSON, rasters) for the dashboard frontend to render via MapLibre,
# not static figures the way the original portfolio project did.
#
# GDAL requires a pre-built wheel on Windows for local dev; inside the
# Docker image (Ubuntu) `pip install GDAL==<version>` matching the
# system's apt-installed GDAL works directly - see Dockerfile.

boto3==1.43.36
botocore==1.43.36
geopandas==1.1.3
httpx==0.28.1
numpy==2.4.2
psycopg2==2.9.12
pyproj==3.7.2
pyroSAR==0.36.2
python-dotenv==1.2.2
PyYAML==6.0.3
rasterio==1.5.0
rasterstats==0.21.0
scipy==1.18.0
shapely==2.1.2
spatialist==0.19.0
